name: eds-geospatial
channels:
  - conda-forge
dependencies:
  - python=3.11
  - jupyterlab
  - uv # For fast pip/git installs
  - just
  - geospatial # Core opengeos stack
  - segment-geospatial # SAM/GeoAI segmentation (samgeo)
  - intake
  - intake-esm

  - pip:
    - git+https://github.com/opengeos/geoai.git # Optional: latest GeoAI tools